@model Projekt.Entities.Cart

  
        <ul class="nav navbar-nav navbar-right">

            <li>@Html.ActionLink("Checkout", "Index", "Cart", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
     
        </ul>


    <div class="navbar-text navbar-right">
        <b>Your cart:</b>
        @Model.Lines.Sum(x => x.Quantity) item(s),
        @Model.ComputeTotalValue().ToString("c")
    </div>


<ul class="nav navbar-nav navbar-right">

    <li>@Html.ActionLink("Log in as Administrator", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    <li>@Html.ActionLink("About us", "About", "Home", routeValues: null, htmlAttributes: new { id = "" })</li>
</ul>

